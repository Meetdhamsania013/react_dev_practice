import styled from "styled-components";

export const APIUserStyled = styled.div`
 width: 100%;
 background-color: ${(props) => props?.theme?.general?.lightreal};
 padding: 62px 28px;
 height: 100vh;
 overflow: auto;
`;
